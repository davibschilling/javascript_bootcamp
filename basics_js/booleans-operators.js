// Declara a variável temp com o numero 32
let temp = 32

// declara a variável isFreezing e retorna true ou false baseado na veracidade da condição
let isFreezing = temp !== 31

console.log(isFreezing)

// === -> operador de igualdade
// !== -> operador de diferença
// < -> operador menor que
// > -> operador maior que
// <= -> operador menor ou igual a
// > -> operador maior ou igual a